From: win7-support-bot <win7@cromite.org>
Date: Sun, 26 Jul 2026 00:00:00 +0000
Subject: WIN7: tp-angle / d3d

Files:
 third_party/angle/src/libANGLE/renderer/d3d/d3d11/Renderer11.cpp

License: GPL-2.0-or-later - https://spdx.org/licenses/GPL-2.0-or-later.html
---
diff --git a/third_party/angle/src/libANGLE/renderer/d3d/d3d11/Renderer11.cpp b/third_party/angle/src/libANGLE/renderer/d3d/d3d11/Renderer11.cpp
index a24fbc52f7..4fb02f183c 100644
--- a/third_party/angle/src/libANGLE/renderer/d3d/d3d11/Renderer11.cpp
+++ b/third_party/angle/src/libANGLE/renderer/d3d/d3d11/Renderer11.cpp
@@ -692,6 +692,27 @@ HRESULT Renderer11::callD3D11CreateDevice(PFN_D3D11_CREATE_DEVICE createDevice,
         &(mRenderer11DeviceCaps.featureLevel), &mDeviceContext);
 }
 
+#include <strsafe.h>
+
+static HMODULE load_dxgi_module() {
+    TCHAR systemPath[MAX_PATH] = TEXT("");
+    GetSystemDirectory(systemPath, MAX_PATH);
+    StringCchCat(systemPath, MAX_PATH, TEXT("\\dxgi.dll"));
+
+    return LoadLibrary(systemPath);
+}
+
+typedef HRESULT (APIENTRY *PFN_CreateDXGIFactory1)(REFIID riid, void **ppFactory);
+
+HRESULT dyn_CreateDXGIFactory1(REFIID riid, void **ppFactory) {
+    PFN_CreateDXGIFactory1 fpCreateDXGIFactory1 =
+        (PFN_CreateDXGIFactory1)GetProcAddress(load_dxgi_module(), "CreateDXGIFactory1");
+
+    if (fpCreateDXGIFactory1 != NULL)
+        return fpCreateDXGIFactory1(riid, ppFactory);
+
+    return DXGI_ERROR_NOT_FOUND;
+}
 egl::Error Renderer11::initializeDXGIAdapter()
 {
     if (mCreatedWithDeviceEXT)
@@ -723,7 +744,7 @@ egl::Error Renderer11::initializeDXGIAdapter()
     else if (mRequestedDriverType == D3D_DRIVER_TYPE_HARDWARE)
     {
         angle::ComPtr<IDXGIFactory1> factory;
-        HRESULT hr = CreateDXGIFactory1(IID_PPV_ARGS(&factory));
+        HRESULT hr = dyn_CreateDXGIFactory1(IID_PPV_ARGS(&factory));
         if (FAILED(hr))
         {
             return egl::Error(EGL_NOT_INITIALIZED, D3D11_INIT_OTHER_ERROR,
@@ -858,7 +879,7 @@ HRESULT Renderer11::callD3D11On12CreateDevice(PFN_D3D12_CREATE_DEVICE createDevi
     else if (mRequestedDriverType == D3D_DRIVER_TYPE_WARP)
     {
         angle::ComPtr<IDXGIFactory4> factory;
-        result = CreateDXGIFactory1(IID_PPV_ARGS(&factory));
+        result = dyn_CreateDXGIFactory1(IID_PPV_ARGS(&factory));
         if (FAILED(result))
         {
             return result;

